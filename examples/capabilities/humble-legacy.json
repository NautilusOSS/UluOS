[
  {
    "name": "humble-legacy.get_pools",
    "service": "humble-legacy",
    "tool": "get_pools",
    "description": "List legacy Humble Swap (duoswap) pools on Algorand with token pairs and pool type (net_tok or tok_tok)",
    "chains": ["algorand-mainnet"],
    "readOnly": true,
    "requiresAuth": false,
    "requiresSigning": false,
    "requiresBroadcast": false,
    "requiresPayment": false,
    "pricingTier": "free",
    "parameters": {
      "type": "object",
      "properties": {
        "symbol": {
          "type": "string",
          "description": "Filter pools containing this token symbol (e.g. ALGO, USDC)"
        }
      }
    },
    "response": {
      "type": "array",
      "description": "Pool list with token pairs and pool type"
    }
  },
  {
    "name": "humble-legacy.get_pool",
    "service": "humble-legacy",
    "tool": "get_pool",
    "description": "Get detailed on-chain info for a specific legacy Humble pool including reserves, token pair, and LP asset",
    "chains": ["algorand-mainnet"],
    "readOnly": true,
    "requiresAuth": false,
    "requiresSigning": false,
    "requiresBroadcast": false,
    "requiresPayment": false,
    "pricingTier": "standard",
    "parameters": {
      "type": "object",
      "required": ["poolId"],
      "properties": {
        "poolId": {
          "type": "number",
          "description": "Pool application ID"
        }
      }
    },
    "response": {
      "type": "object",
      "description": "Pool reserves, token pair, and LP asset details"
    }
  },
  {
    "name": "humble-legacy.get_tokens",
    "service": "humble-legacy",
    "tool": "get_tokens",
    "description": "List ASA tokens traded on legacy Humble Swap pools on Algorand",
    "chains": ["algorand-mainnet"],
    "readOnly": true,
    "requiresAuth": false,
    "requiresSigning": false,
    "requiresBroadcast": false,
    "requiresPayment": false,
    "pricingTier": "free",
    "parameters": {
      "type": "object",
      "properties": {
        "symbol": {
          "type": "string",
          "description": "Filter by token symbol"
        }
      }
    },
    "response": {
      "type": "array",
      "description": "ASA token list with metadata"
    }
  },
  {
    "name": "humble-legacy.get_token_info",
    "service": "humble-legacy",
    "tool": "get_token_info",
    "description": "Get ASA token metadata (name, symbol, decimals, total supply) by asset ID",
    "chains": ["algorand-mainnet"],
    "readOnly": true,
    "requiresAuth": false,
    "requiresSigning": false,
    "requiresBroadcast": false,
    "requiresPayment": false,
    "pricingTier": "free",
    "parameters": {
      "type": "object",
      "required": ["assetId"],
      "properties": {
        "assetId": {
          "type": "number",
          "description": "Algorand Standard Asset ID"
        }
      }
    },
    "response": {
      "type": "object",
      "description": "ASA metadata including name, symbol, decimals, and total supply"
    }
  },
  {
    "name": "humble-legacy.get_quote",
    "service": "humble-legacy",
    "tool": "get_quote",
    "description": "Get a swap quote for legacy Humble Swap on Algorand. Computes expected output, rate, fee, price impact, and minimum received using constant-product AMM math.",
    "chains": ["algorand-mainnet"],
    "readOnly": true,
    "requiresAuth": false,
    "requiresSigning": false,
    "requiresBroadcast": false,
    "requiresPayment": false,
    "pricingTier": "standard",
    "parameters": {
      "type": "object",
      "required": ["fromToken", "toToken", "amount"],
      "properties": {
        "fromToken": {
          "type": "string",
          "description": "From token symbol or ASA ID (e.g. 'ALGO', 'USDC', '31566704')"
        },
        "toToken": {
          "type": "string",
          "description": "To token symbol or ASA ID"
        },
        "amount": {
          "type": "string",
          "description": "Amount to swap in human-readable units (e.g. '100' for 100 ALGO)"
        },
        "slippage": {
          "type": "number",
          "default": 5,
          "description": "Allowed slippage percentage (default 5)"
        }
      }
    },
    "response": {
      "type": "object",
      "description": "Quote with expected output, rate, fee, price impact, and minimum received"
    }
  },
  {
    "name": "humble-legacy.swap_txn",
    "service": "humble-legacy",
    "tool": "swap_txn",
    "description": "Build unsigned transactions to swap tokens on legacy Humble Swap (Algorand). Supports net_tok (ALGO/ASA) and tok_tok (ASA/ASA) pools.",
    "chains": ["algorand-mainnet"],
    "readOnly": false,
    "requiresAuth": true,
    "requiresSigning": true,
    "requiresBroadcast": true,
    "requiresPayment": false,
    "pricingTier": "premium",
    "parameters": {
      "type": "object",
      "required": ["fromToken", "toToken", "amount", "sender"],
      "properties": {
        "fromToken": {
          "type": "string",
          "description": "From token symbol or ASA ID"
        },
        "toToken": {
          "type": "string",
          "description": "To token symbol or ASA ID"
        },
        "amount": {
          "type": "string",
          "description": "Amount to swap in human-readable units"
        },
        "sender": {
          "type": "string",
          "description": "Sender wallet address"
        },
        "slippage": {
          "type": "number",
          "default": 5,
          "description": "Allowed slippage percentage (default 5)"
        }
      }
    },
    "response": {
      "type": "object",
      "description": "Base64-encoded unsigned transactions for signing via UluWalletMCP"
    }
  },
  {
    "name": "humble-legacy.add_liquidity_txn",
    "service": "humble-legacy",
    "tool": "add_liquidity_txn",
    "description": "Build unsigned transactions to add liquidity to a legacy Humble Swap pool on Algorand",
    "chains": ["algorand-mainnet"],
    "readOnly": false,
    "requiresAuth": true,
    "requiresSigning": true,
    "requiresBroadcast": true,
    "requiresPayment": false,
    "pricingTier": "premium",
    "parameters": {
      "type": "object",
      "required": ["poolId", "amountA", "amountB", "sender"],
      "properties": {
        "poolId": {
          "type": "number",
          "description": "Pool application ID"
        },
        "amountA": {
          "type": "string",
          "description": "Amount of Token A in human-readable units (ALGO for net_tok pools)"
        },
        "amountB": {
          "type": "string",
          "description": "Amount of Token B in human-readable units"
        },
        "sender": {
          "type": "string",
          "description": "Sender wallet address"
        }
      }
    },
    "response": {
      "type": "object",
      "description": "Base64-encoded unsigned transactions for signing"
    }
  },
  {
    "name": "humble-legacy.remove_liquidity_txn",
    "service": "humble-legacy",
    "tool": "remove_liquidity_txn",
    "description": "Build unsigned transactions to remove liquidity from a legacy Humble Swap pool on Algorand",
    "chains": ["algorand-mainnet"],
    "readOnly": false,
    "requiresAuth": true,
    "requiresSigning": true,
    "requiresBroadcast": true,
    "requiresPayment": false,
    "pricingTier": "premium",
    "parameters": {
      "type": "object",
      "required": ["poolId", "lpAmount", "sender"],
      "properties": {
        "poolId": {
          "type": "number",
          "description": "Pool application ID"
        },
        "lpAmount": {
          "type": "string",
          "description": "Amount of LP tokens to burn in human-readable units"
        },
        "sender": {
          "type": "string",
          "description": "Sender wallet address"
        }
      }
    },
    "response": {
      "type": "object",
      "description": "Base64-encoded unsigned transactions for signing"
    }
  }
]
